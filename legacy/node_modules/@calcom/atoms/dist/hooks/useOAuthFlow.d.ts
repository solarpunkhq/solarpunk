export interface useOAuthProps {
    accessToken?: string;
    refreshUrl?: string;
    onError?: (error: string) => void;
    onSuccess?: () => void;
    clientId: string;
}
export declare const useOAuthFlow: ({ accessToken, refreshUrl, clientId, onError, onSuccess }: useOAuthProps) => {
    isRefreshing: boolean;
    currentAccessToken: string;
};
//# sourceMappingURL=useOAuthFlow.d.ts.map